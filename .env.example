# ============================================
# Docker Compose Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# Database Configuration
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=acta_db  # REQUIRED: Database name
POSTGRES_PORT=5432

# ============================================
# Backend Configuration
# ============================================
# JWT Secret (REQUIRED for production)
# Generate a strong random secret: openssl rand -base64 32
# Minimum 32 characters recommended
JWT_SECRET=dl4w2onoEp5Z0YMA70WUnYDJibsm8MiUQpG1iKLGYP0=

# Backend Port
BACKEND_PORT=3000

# API URL (optional, for generating absolute URLs in invoices)
# Used in production for invoice logo URLs
API_URL=http://localhost:3000

# ============================================
# Frontend Configuration
# ============================================
# Frontend Port
# Development: 5173 (Vite dev server)
# Production: 80 (nginx)
FRONTEND_PORT=80

# API URL for frontend (optional)
# For local development, frontend auto-detects the backend URL
# For production, set this to your actual backend URL
# Example: https://api.yourdomain.com
VITE_API_URL=http://localhost:3000

# ============================================
# Environment
# ============================================
# Set to 'production' for production deployment
# Set to 'development' for development
NODE_ENV=production

# ============================================
# Notes
# ============================================
# 1. For production, ensure JWT_SECRET is set to a strong, random value
# 2. Update VITE_API_URL to match your production backend URL
# 3. Use strong, unique passwords for POSTGRES_PASSWORD
# 4. Never commit the .env file to version control
# 5. Backend and Frontend also have their own .env.example files:
#    - backend/.env.example
#    - frontend/.env.example
#    Copy and configure those as well for full functionality
